mutation LiteralClubReadingStates($token: String!) {
    getReadingStates(token: $token) {
        book {
            title
            description
            cover
            authors {
                name
            }
        }
        status
    }
}